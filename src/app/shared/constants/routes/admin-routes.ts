export const ADMIN_PATHS = {
  base: 'admin',
  declarations: 'declarations',
  games: 'games',
};

export const ADMIN_ROUTES = {
  declarations: `/${ADMIN_PATHS.base}/${ADMIN_PATHS.declarations}`,
  games: `/${ADMIN_PATHS.base}/${ADMIN_PATHS.games}`,
};
